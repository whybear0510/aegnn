# from distutils.core import setup
from setuptools import setup, find_packages

setup(name='aegnn',
      # packages=['aegnn']
      # packages=find_packages('aegnn')
      packages=find_packages()
      )
